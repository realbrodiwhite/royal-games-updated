
module.exports = {
    webpack: {
        configure: (webpackConfig) => {
            // Custom Webpack configuration
            return webpackConfig;
        },
    },
    devServer: {
        // Default configuration
    },
};
